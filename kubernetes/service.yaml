apiVersion: v1
kind: Service
metadata:
  name: quoterxp-service
spec:
  selector:
    app: quoterxp
  type: NodePort
  ipFamilyPolicy: PreferDualStack
  ports:
  - protocol: TCP
    port: 5000
    nodePort: 30000
    targetPort: 5000
